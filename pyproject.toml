[project]
name = "llm-generic-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "arpan-lab", email = "chakrabartyarpan8@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "langchain-huggingface>=1.2.2",
    "sentence-transformers>=5.7.0",
]

[project.scripts]
llm-generic-app = "llm_generic_app:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
